const numbers = [1, 2, 3, 4, 5];

const find = numbers.findIndex(n => n === 3) // 2

console.log(find);

// O método fidIndex irá retorna a posição do elemento do array com base na condição estabelicida
// caso nao encontre ira retora -1