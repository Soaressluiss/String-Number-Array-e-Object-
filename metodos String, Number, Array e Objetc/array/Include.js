const numbers = [1, 2, 3, 4, 5];

const includeNumber = numbers.includes(5) // TRUE

console.log(includeNumber);

// O método Include verifica se o elemento passado existe no array
// ele retona TRUE ou FALSE
