const numbers = [1, 2, 3, 4, 5, 6];

const result = numbers.map(Element => {
    return Element * 2 `${Element}`
})

console.log(result);

// O método Map() retorna um novo array baseado no array original;